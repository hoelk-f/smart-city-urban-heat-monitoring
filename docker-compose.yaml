version: '3.8'

services:
  heat-monitoring-app:
    build:
      context: .
      dockerfile: Dockerfile.angular
    ports:
      - "4200:4200"
    volumes:
      - ./src:/app/src
    environment:
      - NODE_ENV=production

  # Optional: python-weather service
  # python-weather:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.python
  #   volumes:
  #     - ./src:/app/src
  #   environment:
  #     - API_KEY=your_api_key_here
  #   tty: true
